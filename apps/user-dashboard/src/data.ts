import type { CustomerProfile, QADialogue } from './types';

/** Зарезервировано: демо-данные убраны — всё из API и импорта. */
export const mockUniversalAudience: CustomerProfile[] = [];

export const mockAIDialogues: QADialogue[] = [];
